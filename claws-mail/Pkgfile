# Description: An e-mail client (and news reader), based on GTK+ 
# URL:         http://www.claws-mail.org/index.php
# Maintainer:  John Vogel, jvogel4 at stny dot rr dot com
# Depends on:  gtk libetpan gpgme gnutls
# Nice to have: enchant clamav dillo openldap poppler python webkitgtk

name=claws-mail
version=3.15.1
release=1
source=(http://www.claws-mail.org/releases/$name-$version.tar.xz)

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--disable-static \
		--disable-nls \
		--disable-networkmanager --disable-aspell --disable-enchant \
		--disable-manual --without-aspell

	make
	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share/doc
}
