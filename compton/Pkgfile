# Description: A compositor for X, and a fork of xcompmgr-dana.
# URL: https://github.com/yshui/compton
# Maintainer: Tim Biermann, tbier at posteo dot de
# Packager: Danny Rawlins, crux at romster dot me
# Depends on: dbus libconfig libev libpcre libxdg-basedir mesa3d uthash xorg-libxcomposite xorg-libxinerama xorg-libpixman xorg-libxrandr xorg-xcb-util-image xorg-xcb-util-renderutil

name=compton
version=7.2
release=1
source=(https://github.com/yshui/compton/archive/v$version/$name-$version.tar.gz skip_tests.patch)

build() {
  cd $name-$version
  patch -Np1 -i $SRC/skip_tests.patch
  mkdir build
  meson . build \
    --prefix=/usr \
    --buildtype=release \
    -Dbuild_docs=false
  ninja -v -C build
  DESTDIR=$PKG ninja -C build install
}
