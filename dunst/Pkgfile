# Description: Dunst is a lightweight replacement for the notification-daemons provided by most desktop environments
# URL: https://dunst-project.org/
# Maintainer: Tim Biermann, tbier at posteo dot de
# Depends on: libxdg-basedir

name=dunst
version=1.4.1
release=1
source=(https://github.com/dunst-project/$name/archive/v$version/$name-v$version.tar.gz)

build() {
  cd $name-$version
  make
  make DESTDIR=$PKG PREFIX=/usr install
}
