# Description: FASTCgi (fcgi) is a language independent, high performant extension to CGI
# URL: http://www.fastcgi.com/
# Maintainer: Tim Biermann, tbier at posteo dot de
# Depends on: 

name=fcgi
version=2.4.0
release=1
source=(
https://sources.archlinux.org/other/packages/fcgi/$name-$version.tar.gz
fcgi-2.4.0-configure.in.patch
fcgi-2.4.0-Makefile.am-CPPFLAGS.patch
fcgi-2.4.0-gcc44_fixes.patch
fcgi-2.4.0-poll.patch
)

build() {
	cd $name-$version
    for i in ../*.patch; do
        patch -p1 -i $i;
    done
    libtoolize --automake --copy --force
    aclocal
    autoheader
    automake --add-missing --force-missing --copy
    autoconf
    ./configure --prefix=/usr
    make
    make DESTDIR=$PKG install
}
