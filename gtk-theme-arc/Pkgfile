# Description: A gtk theme
# URL: https://github.com/horst3180/arc-theme
# Maintainer: Tim Biermann, tbier at posteo dot de
# Depends on: gtk gtk-engine-murrine sassc optipng inkscape
# Optional: arc-icon-theme

name=gtk-theme-arc
version=20190213
release=1
source=(https://github.com/NicoHood/arc-theme/archive/$version/arc-theme-$version.tar.gz)

build() {
	cd arc-theme-$version
    ./autogen.sh --prefix=/usr \
                --disable-xfwm \
                --disable-unity \
                --disable-gnome-shell \
                --disable-metacity \
                --disable-plank
#    sed -i s/export-png/export-type/g common/gtk-2.0/Makefile.*
    #sed -i s/export-png/export-type/g common/gtk-2.0/Makefile.*
#    sed -i s/export-png/export-type/g common/gtk-3.0/3.20/Makefile.*
#    sed -i s/export-png/export-type/g common/gtk-3.0/3.18/Makefile.*
    make
	make DESTDIR=$PKG install
    rm -fr $PKG/usr/share/themes/*/cinnamon
}
