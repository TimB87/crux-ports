# Description: Python bindings for the Linux input subsystem
# URL: https://pypi.org/project/evdev/
# Maintainer: Tim Biermann, tbier at posteo dot de
# Depends on: python-setuptools

name=python-evdev
version=1.4.0
release=2
source=(https://pypi.python.org/packages/source/e/evdev/evdev-$version.tar.gz)

build() {
  cd evdev-$version
  /usr/bin/python2 setup.py build
  /usr/bin/python2 setup.py install --prefix=/usr \
    --root=$PKG \
    -O1 \
    --skip-build
}
