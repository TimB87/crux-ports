# Description:
# URL:
# Maintainer:
# Depends on:

name=slang
version=2.3.1a
release=1
source=(https://www.jedsoft.org/releases/slang/slang-2.3.1a.tar.bz2)

build() {
	cd $name-$version
	./configure --prefix=/usr --disable-nls
	make
	make DESTDIR=$PKG install
}
