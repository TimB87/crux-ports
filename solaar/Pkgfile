# Description: Another pairing tool for Logitech devices
# URL: https://github.com/pwr/Solaar
# Maintainer: Tim Biermann, tbier at posteo dot de
# Depends on: python3-pyudev

name=solaar
version=git
release=2
source=()
git=(https://github.com/pwr/Solaar.git)

build() {
	git clone $git $name
    cd $name
    python3 setup.py install --root=$PKG --optimize=1
    install -D -m0644 \
        rules.d/42-logitech-unify-permissions.rules \
        $PKG/etc/udev/rules.d/42-logitech-unify-permissions.rules
}
