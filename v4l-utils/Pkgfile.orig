# Description: Userspace tools and conversion library for Video 4 Linux
# URL: https://linuxtv.org/
# Maintainer: Tim Biermann, tbier at posteo dot de
# Depends on: alsa-lib expat glib harfbuzz icu libffi libjpeg-turbo libpng libxml2 llvm qt5 libdrm mesa3d xorg-libx11 xorg-libxau xorg-libxcb xorg-libxdamage xorg-libxdmcp xorg-libxext xorg-libxfixes xorg-libxshmfence xorg-libxxf86vm 

name=v4l-utils
version=1.14.2
release=1
source=(https://linuxtv.org/downloads/v4l-utils/$name-$version.tar.bz2)

build() {
	cd $name-$version
    ./configure --prefix=/usr \
                --disable-nls
    make
    MAKEFLAGS="-j1" make install DESTDIR=$PKG
    rm $PKG/usr/bin/ivtv-ctl
}
