# Description: A GTK+ backend for xdg-desktop-portal
# URL: https://github.com/flatpak/xdg-desktop-portal-gtk
# Maintainer: Tim Biermann, tbier at posteo dot de
# Depends on: atk at-spi2-atk at-spi2-core cairo dbus expat gdk-pixbuf glib gtk3 harfbuzz libffi libpng pango fontconfig freetype fribidi libepoxy xorg-libpixman xorg-libx11 xorg-libxau xorg-libxcb xorg-libxcomposite xorg-libxcursor xorg-libxdamage xorg-libxdmcp xorg-libxext xorg-libxfixes xorg-libxinerama xorg-libxi xorg-libxrandr xorg-libxrender

name=xdg-desktop-portal-gtk
version=0.11
release=1
source=(https://github.com/flatpak/xdg-desktop-portal-gtk/releases/download/$version/$name-$version.tar.xz)

build() {
	cd $name-$version

    ./configure --prefix=/usr \
                --libexecdir=/usr/lib \
                --disable-nls
    
    make
    make DESTDIR=$PKG install
}
