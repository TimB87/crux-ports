# Description: Desktop integration portals for sandboxed apps
# URL: https://github.com/flatpak/xdg-desktop-portal
# Maintainer: Tim Biermann, tbier at posteo dot de
# Depends on: fuse json-glib libportal pipewire

name=xdg-desktop-portal
version=1.6.0
release=2
source=(https://github.com/flatpak/xdg-desktop-portal/releases/download/$version/$name-$version.tar.xz
  pipewire-0.3.0.patch)

build() {
  cd $name-$version
  patch -Np1 -i $SRC/pipewire-0.3.0.patch
  autoreconf -vfi
  ./configure --prefix=/usr \
    --libexecdir=/usr/lib \
    --disable-geoclue \
    --enable-pipewire \
    --disable-nls
  make
  make DESTDIR=$PKG install
  rm -fr $PKG/usr/{lib/systemd,share/doc}
}
