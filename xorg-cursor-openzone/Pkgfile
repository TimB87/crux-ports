# Description: OpenZone mouse cursor theme for X11
# URL: https://github.com/ducakar/openzone-cursors
# Maintainer: Tim Biermann <tim_biermann@web.de>
# Depends on: icon-slicer

name=xorg-cursor-openzone
version=1.2.6
release=1
source=(https://github.com/ducakar/openzone-cursors/archive/v$version/openzone-cursors-$version.tar.gz)

build() {
	cd openzone-cursors-$version
    make
    mkdir -p $PKG/usr/share/icons
    for i in OpenZone_*.tar.*; do
        tar xfv $i -C $PKG/usr/share/icons
    done
}
